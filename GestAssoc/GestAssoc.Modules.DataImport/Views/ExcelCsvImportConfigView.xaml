<UserControl x:Class="GestAssoc.Modules.DataImport.Views.ExcelCsvImportConfigView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<StackPanel Orientation="Vertical">
		<DataGrid HorizontalAlignment="Left" ItemsSource="{Binding Path=ColumnsMapping}" AutoGenerateColumns="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Donnée" IsReadOnly="True" Binding="{Binding Path=DataName}" />
				<DataGridTextColumn Header="Colonne" IsReadOnly="False" Binding="{Binding Path=ColumnHeader}" />
			</DataGrid.Columns>
		</DataGrid>

		<Button Command="{Binding Path=ReadFileCmd}">GO</Button>
		</StackPanel>
	</Grid>
</UserControl>

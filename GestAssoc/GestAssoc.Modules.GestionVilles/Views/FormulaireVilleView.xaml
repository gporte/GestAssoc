<UserControl x:Class="GestAssoc.Modules.GestionVilles.Views.FormulaireVilleView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:wpf="http://schemas.xceed.com/wpf/xaml/toolkit"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="500">
	<UserControl.Resources>
		<Style x:Key="titreUC" TargetType="{x:Type Label}">
			<Setter Property="FontSize" Value="18" />
			<Setter Property="FontWeight" Value="Bold" />
		</Style>

		<Style x:Key="lblForm" TargetType="{x:Type Label}">
			<Setter Property="Width" Value="100" />
			<Setter Property="Height" Value="25" />
		</Style>

		<Style x:Key="tbxFormBig" TargetType="{x:Type TextBox}">
			<Setter Property="Width" Value="200" />
			<Setter Property="Height" Value="25" />
			<Setter Property="Margin" Value="0,5,0,5" />
		</Style>

		<Style x:Key="tbxFormLittle" TargetType="{x:Type TextBox}">
			<Setter Property="Width" Value="100" />
			<Setter Property="Height" Value="25" />
			<Setter Property="Margin" Value="0,5,0,5" />
		</Style>

		<Style x:Key="btnCmd" TargetType="{x:Type Button}">
			<Setter Property="Height" Value="35" />
			<Setter Property="Width" Value="35" />
			<Setter Property="Margin" Value="10,0,10,0" />
		</Style>
	</UserControl.Resources>
	
	<DockPanel>
		<Label DockPanel.Dock="Top" Content="Ville" HorizontalContentAlignment="Center" 
			   Style="{StaticResource titreUC}"/>

		<WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" VerticalAlignment="Bottom">
			<Button Style="{StaticResource btnCmd}" ToolTip="Retour"
					Command="{Binding Path=CancelCmd}">
				<Image Source="Images\btn_back.png" />
			</Button>
			
			<Button Style="{StaticResource btnCmd}" ToolTip="Enregistrer"
					Command="{Binding Path=SaveCmd}" CommandParameter="{Binding Path=Item}">
				<Image Source="Images\btn_save.png" />
			</Button>
		</WrapPanel>

		<WrapPanel Orientation="Vertical" HorizontalAlignment="Center">
			<StackPanel Orientation="Horizontal">
				<Label Content="Nom : " Style="{StaticResource lblForm}" />
				<TextBox Text="{Binding Item.Libelle}" Style="{StaticResource tbxFormBig}" CharacterCasing="Upper"/>
			</StackPanel>

			<StackPanel Orientation="Horizontal">
				<Label Content="Code postal : " Style="{StaticResource lblForm}" />
				<wpf:MaskedTextBox Mask="00000" Text="{Binding Item.CodePostal}" 
								   Style="{StaticResource tbxFormLittle}"/>
			</StackPanel>
		</WrapPanel>
	</DockPanel>
</UserControl>
